namespace Identity.Domain.Entities.Enums
{
    /// <summary> Static roles used in Identity </summary>
    public enum Role
    {
        Admin = 1,
        User = 2,
        ApiClient = 3,
        Guest = 4
    }
}
